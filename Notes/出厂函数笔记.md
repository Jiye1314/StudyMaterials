#### 定时器轮询函数

```c++
TForm1::Timer1Timer(TObject *Sender);
```

#### 检测启动信号线程函数

```c++
ReadStartSignalThread(Pointer Parameter);
```

#### 读取PLC启动信号函数

```c++
ReadStartSignal();
```

#### Modbus TCP 读取线圈地址函数

```c++
readInput_retry(Port, 1, &value);
```

#### 检测单/三项旋钮是否匹配

```c++
IfDanSanXiangXuanNiuAndSetMatched()
```

```c++
bool TForm1::IfDanSanXiangXuanNiuAndSetMatched(){}
```



#### 加载电机型号参数函数

```c++
SetAccordingToTestParameters();
```

#### 数据清零函数

```c++
InitAllTestData();
```

#### 界面刷新函数

```c++
InitAllDisplayPanelsAndLabels();
```

